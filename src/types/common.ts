export type ClassName = {
  className?: string;
};

export type Children = {
  children?: React.ReactNode;
};

export type Fn = (...args: any[]) => any;

export type Option = {
  label: JSX.Element;
  value: string;
};
